- name: ZSH | Install
  dnf:
    name:
      - zsh
      - util-linux-user
      - powerline-fonts
    state: present
  become: true

- name: ZSH | Set as default shell
  shell: chsh -s $(which zsh ) {{ user }}
