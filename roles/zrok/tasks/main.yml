---
- name: Get Latest Release
  community.general.github_release:
    action: latest_release
    user: openziti
    repo: zrok
    token: "{{ github_token }}"
  register: zrok_release

- name: Download
  ansible.builtin.unarchive:
    src: "https://github.com/openziti/zrok/releases/download/{{ zrok_release.tag }}/zrok_{{ zrok_release.tag[1:] }}_linux_amd64.tar.gz"
    dest: "{{ setup_dir }}"
    remote_src: true

- name: Install
  ansible.builtin.copy:
    src: "{{ setup_dir }}/zrok"
    dest: "{{ bin_dir }}"
    mode: "a+x"
  become: true
