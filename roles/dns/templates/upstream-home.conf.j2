# Home DNS server. Rewritten automatically by dns-home-probe based on
# reachability (LAN at home, or via Tailscale away with accept-routes) -
# this deploy-time value is just the initial state.
server={{ dns_upstream_servers[0] }}
